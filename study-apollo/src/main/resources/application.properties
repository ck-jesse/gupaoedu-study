app.id=shop-web-client
# set apollo meta server address, adjust to actual address if necessary
# 此处必须配置meta-server的地址（一般为IP:8080），不能是portal-service的地址（一般为IP:8070）
# apollo演示
#apollo.meta=http://106.12.25.204:8080
# akulaku
apollo.meta=http://172.31.1.122:8080
# will inject 'application' namespace in bootstrap phase
apollo.bootstrap.enabled=true
apollo.bootstrap.namespaces=application,application.yml